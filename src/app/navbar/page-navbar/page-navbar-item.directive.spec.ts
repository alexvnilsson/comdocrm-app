import { PageNavbarItemDirective } from './page-navbar-item.directive';

describe('PageNavbarItemDirective', () => {
  it('should create an instance', () => {
    const directive = new PageNavbarItemDirective();
    expect(directive).toBeTruthy();
  });
});
