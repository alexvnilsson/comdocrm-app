import { WordSplicePipe } from './word-splice.pipe';

describe('WordSplicePipe', () => {
  it('create an instance', () => {
    const pipe = new WordSplicePipe();
    expect(pipe).toBeTruthy();
  });
});
