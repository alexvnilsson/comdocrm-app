import { TabsTabDirective } from './tabs-tab.directive';

describe('TabsTabDirective', () => {
  it('should create an instance', () => {
    const directive = new TabsTabDirective();
    expect(directive).toBeTruthy();
  });
});
