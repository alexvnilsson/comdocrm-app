import { ModalWrapperDirective } from './modal-wrapper.directive';

describe('ModalWrapperDirective', () => {
  it('should create an instance', () => {
    const directive = new ModalWrapperDirective();
    expect(directive).toBeTruthy();
  });
});
